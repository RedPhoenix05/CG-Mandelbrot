# Copilot Instructions

## Project Guidelines
- User prefers Mandelbrot parameters and controls (zoom rate, center, colors) to be tunable through a configuration file without recompiling, and prefers shader code in external files instead of embedded raw strings.